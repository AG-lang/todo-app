services:
  - type: web
    name: flask-todo-app
    env: python
    plan: free
    buildCommand: "./.render/build.sh"
    startCommand: "gunicorn app:app"
    envVars:
      - key: FLASK_ENV
        value: production